# Extend partition


# Reset root password

# Connect to wifi from CMD
iwconfig wlan0 essid name key password
dhclient wlan0
http://www.linuxjournal.com/content/wi-fi-command-line

# USB network driver
install https://wiki.debian.org/ath9k_htc
lsusb -
dmesg - warnings
apt-get update && apt-get install firmware-atheros
sudo apt-get install firmware-atheros 
sudo apt-get install firmware-linux-free

 Razer Chroma